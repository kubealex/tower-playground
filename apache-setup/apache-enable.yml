- name: Greeter playbook
  hosts: webserver
  tasks:
    - name: This task ensures httpd package is present on {{ ansible_hostname }}
      service:
        name: httpd
        state: started
        enabled: true
      become: true
